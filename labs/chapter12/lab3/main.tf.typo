# Copy over probe.tf and run `terraform validate`.
# A block type that simply does not exist. Compare this error with the one from
# main.tf.lifecycle -- they are identical in form.
resource "terraform_data" "limit" {
  input = "x"

  dynamic "nonexistent" {
    for_each = ["a"]
    content {}
  }
}
