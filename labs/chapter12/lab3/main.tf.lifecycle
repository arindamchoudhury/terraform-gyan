# Copy over probe.tf and run `terraform validate`.
# Terraform must process lifecycle before it is safe to evaluate expressions,
# so the block cannot be generated by one.
resource "terraform_data" "limit" {
  input = "x"

  dynamic "lifecycle" {
    for_each = ["a"]
    content {}
  }
}
