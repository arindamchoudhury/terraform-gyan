# Shell scripts must keep LF endings even when checked out on Windows.
# With core.autocrlf=true a CRLF checkout makes `source labs/lab-env.sh` export
# values with a trailing carriage return, which silently breaks the endpoint it
# is meant to fix.
*.sh   text eol=lf
.envrc text eol=lf
