site/
__pycache__/
*.pyc
.venv/
.claude/

# Working files for PDF note-taking (raw extracted book text — don't publish/commit)
docs/sources/*/pdf-cache/
docs/books/*/pdf-cache/

# Cached web fetches (fetch_page.py output — research scratch, don't commit)
cache/

# Terraform
**/.terraform/*
*.tfstate
*.tfstate.*
*.tfplan
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# Terraform secrets (lab .tfvars stay committed on purpose)
*.auto.tfvars
secrets.tfvars
